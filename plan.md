# 训练改进计划：长期运行模型

## 目标

让模型具备完整的飞行能力闭环：**快速飞向目标 → 减速到达 → 悬停待命 → 悬停中避障 → 回位继续待命**，支持长期自主运行。

部署目标：NVIDIA Orin NX 16G，实时推理 < 1ms @ 15Hz。

## 现状

- 模型架构：CNN + GRU，dim_obs=10，dim_action=6，hidden=192
- 训练行为：高速穿越航点（speed_mtp=4，目标速度 [3, 13] m/s）
- 问题：模型没学过"减速 → 悬停"，到达目标后在 1m↔3.7m 间震荡打转

## 训练环境改动

### 1. 速度调度（Speed Schedule）

将固定 `speed_mtp` 替换为距离相关的目标速度：

```python
# 现在: target_speed = speed_mtp * (1 + rand)  → 恒定高速
# 改为:
target_speed = min(speed_mtp * (1 + rand), k * dist)
# dist 近时 target_speed 自然衰减到 ~0
```

`target_v_raw = normalize(p_target - p) * target_speed` 在接近目标时趋向零向量，模型学到减速停止。

### 2. 到达后保持目标 + 持续训练

当前训练中到达航点立即切换下一个。改为到达后**保持当前目标一段时间**（2-5 秒），期间：

- 障碍物继续随机出现/靠近
- 模型需要学会：
  - 悬停（target_v ≈ 0 时输出低推力维持高度）
  - 悬停中避障（深度图检测到障碍 → 闪避）
  - 避障后回位（障碍过去后 target_v 再次指向目标 → 回到目标附近）

## 不需要改动的部分

| 方面 | 理由 |
|------|------|
| 模型架构 | GRU 隐状态天然编码飞行/悬停模式切换；dist 信息隐含在 target_v 模长中 |
| 推理管线 | inference_node.py 完全适用，无需外挂悬停控制器 |
| 部署硬件 | Orin NX 16G 绰绰有余 |

## 前置工作

- [ ] **项目整理**：当前代码结构混乱，缺少测试
- [ ] 补充测试用例
- [ ] 整理完毕后再进行训练改动
