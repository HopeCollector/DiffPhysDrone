[project]
name = "quadsim-cuda"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.8.0",
]

[build-system]
requires = [
    "setuptools>=80.9.0", 
    "wheel>=0.45.1",
    "torch>=2.8.0",
    "numpy>=2.3.2"
]
build-backend = "setuptools.build_meta"
